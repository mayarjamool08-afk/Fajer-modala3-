<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>فجر مدلع</title>
<link href="https://fonts.googleapis.com/css2?family=Tajawal:wght@400;700;900&family=Cairo:wght@400;700;900&family=Rubik+Bubbles&family=Reem+Kufi+Fun:wght@400;700&display=swap" rel="stylesheet">
<style>
  * { margin: 0; padding: 0; box-sizing: border-box; }

  body {
    background: #0a0010;
    width: 100vw;
    height: 100vh;
    overflow: hidden;
    position: relative;
  }

  body::before {
    content: '';
    position: fixed;
    inset: 0;
    background: 
      radial-gradient(ellipse at 20% 50%, #1a0030 0%, transparent 50%),
      radial-gradient(ellipse at 80% 20%, #00001a 0%, transparent 50%),
      radial-gradient(ellipse at 60% 80%, #0d0020 0%, transparent 50%);
    animation: bgPulse 8s ease-in-out infinite alternate;
  }

  @keyframes bgPulse {
    0% { opacity: 0.7; }
    100% { opacity: 1; }
  }

  .text {
    position: absolute;
    white-space: nowrap;
    cursor: pointer;
    user-select: none;
    will-change: transform, left, top;
  }

  .t1 {
    font-family: 'Rubik Bubbles', cursive;
    font-size: 3.5rem;
    animation: move1 9s linear infinite, spin1 4s ease-in-out infinite alternate, colorCycle1 3s linear infinite;
  }

  .t2 {
    font-family: 'Cairo', sans-serif;
    font-weight: 900;
    font-size: 4.5rem;
    color: #ff4400;
    text-shadow: 0 0 20px #ff8800, 0 0 50px #ff4400, 0 0 80px #ff0000;
    animation: move2 7s linear infinite, bounce2 1.5s ease-in-out infinite alternate, fireFlicker 0.3s ease-in-out infinite alternate;
  }

  .t3 {
    font-family: 'Tajawal', sans-serif;
    font-weight: 900;
    font-size: 5rem;
    color: transparent;
    -webkit-text-stroke: 2px #00ffcc;
    text-shadow: 0 0 20px #00ffcc, 0 0 60px #00ffcc;
    animation: move3 11s linear infinite, neonPulse 1s ease-in-out infinite alternate;
  }

  .t4 {
    font-family: 'Reem Kufi Fun', sans-serif;
    font-size: 3rem;
    background: linear-gradient(90deg, #ff00ff, #00ffff, #ffff00, #ff00ff);
    background-size: 300% 100%;
    -webkit-background-clip: text;
    background-clip: text;
    -webkit-text-fill-color: transparent;
    animation: move4 13s linear infinite, rainbowShift 2s linear infinite, wiggle4 2s ease-in-out infinite;
    filter: drop-shadow(0 0 10px #ff00ff);
  }

  .t5 {
    font-family: 'Cairo', sans-serif;
    font-weight: 700;
    font-size: 3.8rem;
    color: #ffd700;
    text-shadow:
      4px 4px 0 #b8860b,
      8px 8px 0 #8b6914,
      0 0 30px #ffd700,
      0 0 60px #ffaa00;
    animation: move5 10s linear infinite, tilt5 3s ease-in-out infinite alternate;
  }

  .t6 {
    font-family: 'Tajawal', sans-serif;
    font-size: 2.8rem;
    color: #aae6ff;
    text-shadow: 0 0 15px #00aaff, 0 0 40px #0066ff, 0 0 70px #0033ff;
    animation: move6 8s linear infinite, shake6 0.4s ease-in-out infinite;
  }

  .t7 {
    font-family: 'Rubik Bubbles', cursive;
    font-size: 4rem;
    color: #ff3366;
    animation: move7 6s linear infinite, glitch 0.8s ease-in-out infinite;
  }

  @keyframes move1 {
    0%   { left: 5%;  top: 10%; }
    20%  { left: 60%; top: 70%; }
    40%  { left: 30%; top: 5%;  }
    60%  { left: 70%; top: 80%; }
    80%  { left: 10%; top: 50%; }
    100% { left: 5%;  top: 10%; }
  }

  @keyframes move2 {
    0%   { left: 50%; top: 5%;  }
    25%  { left: 85%; top: 45%; }
    50%  { left: 40%; top: 80%; }
    75%  { left: 5%;  top: 45%; }
    100% { left: 50%; top: 5%;  }
  }

  @keyframes move3 {
    0%   { left: 2%;  top: 5%;  }
    14%  { left: 50%; top: 20%; }
    28%  { left: 10%; top: 40%; }
    42%  { left: 60%; top: 55%; }
    57%  { left: 5%;  top: 70%; }
    71%  { left: 55%; top: 85%; }
    85%  { left: 20%; top: 60%; }
    100% { left: 2%;  top: 5%;  }
  }

  @keyframes move4 {
    0%   { left: 70%; top: 60%; }
    16%  { left: 10%; top: 20%; }
    33%  { left: 80%; top: 10%; }
    50%  { left: 5%;  top: 75%; }
    66%  { left: 55%; top: 30%; }
    83%  { left: 25%; top: 88%; }
    100% { left: 70%; top: 60%; }
  }

  @keyframes move5 {
    0%   { left: 20%; top: 30%; }
    25%  { left: 65%; top: 15%; }
    50%  { left: 50%; top: 60%; }
    75%  { left: 10%; top: 75%; }
    100% { left: 20%; top: 30%; }
  }

  @keyframes move6 {
    0%   { left: 40%; top: 50%; }
    10%  { left: 80%; top: 10%; }
    20%  { left: 5%;  top: 30%; }
    30%  { left: 60%; top: 80%; }
    40%  { left: 15%; top: 5%;  }
    50%  { left: 75%; top: 55%; }
    60%  { left: 30%; top: 85%; }
    70%  { left: 85%; top: 25%; }
    80%  { left: 2%;  top: 65%; }
    90%  { left: 55%; top: 15%; }
    100% { left: 40%; top: 50%; }
  }

  @keyframes move7 {
    0%   { left: 5%;  top: 5%;  }
    15%  { left: 45%; top: 15%; }
    30%  { left: 75%; top: 40%; }
    45%  { left: 60%; top: 70%; }
    60%  { left: 25%; top: 80%; }
    75%  { left: 5%;  top: 55%; }
    90%  { left: 30%; top: 30%; }
    100% { left: 5%;  top: 5%;  }
  }

  @keyframes spin1 {
    0%   { transform: rotate(-15deg) scale(1); }
    100% { transform: rotate(15deg) scale(1.2); }
  }

  @keyframes bounce2 {
    0%   { transform: translateY(0) scale(1); }
    100% { transform: translateY(-20px) scale(1.1); }
  }

  @keyframes fireFlicker {
    0%   { filter: brightness(1); }
    100% { filter: brightness(1.5) hue-rotate(20deg); }
  }

  @keyframes neonPulse {
    0%   { opacity: 0.7; }
    100% { opacity: 1; filter: blur(0.5px); }
  }

  @keyframes rainbowShift {
    0%   { background-position: 0% 50%; }
    100% { background-position: 100% 50%; }
  }

  @keyframes wiggle4 {
    0%   { transform: rotate(-8deg); }
    50%  { transform: rotate(8deg); }
    100% { transform: rotate(-8deg); }
  }

  @keyframes tilt5 {
    0%   { transform: skewX(-10deg) scale(0.9); }
    100% { transform: skewX(10deg) scale(1.1); }
  }

  @keyframes shake6 {
    0%   { transform: translate(0,0); }
    25%  { transform: translate(5px,-5px); }
    50%  { transform: translate(-5px,5px); }
    75%  { transform: translate(5px,5px); }
    100% { transform: translate(0,0); }
  }

  @keyframes glitch {
    0%   { text-shadow: 3px 0 #00ffff, -3px 0 #ff00ff; transform: none; }
    20%  { text-shadow: -3px 0 #00ffff, 3px 0 #ff00ff; transform: skewX(10deg); }
    40%  { text-shadow: 3px 0 #ff0000, -3px 0 #0000ff; transform: skewX(-5deg); }
    60%  { text-shadow: 0 3px #00ff00, 0 -3px #ff00ff; transform: none; }
    80%  { text-shadow: -3px 0 #ff4400, 3px 0 #00aaff; transform: scale(1.1); }
    100% { text-shadow: 3px 0 #00ffff, -3px 0 #ff00ff; transform: none; }
  }

  @keyframes colorCycle1 {
    0%   { color: #ff6ec7; text-shadow: 0 0 20px #ff6ec7, 0 0 40px #ff6ec7; }
    20%  { color: #ff3366; text-shadow: 0 0 20px #ff3366, 0 0 40px #ff3366; }
    40%  { color: #ff9900; text-shadow: 0 0 20px #ff9900, 0 0 40px #ff9900; }
    60%  { color: #aa00ff; text-shadow: 0 0 20px #aa00ff, 0 0 40px #aa00ff; }
    80%  { color: #00ffcc; text-shadow: 0 0 20px #00ffcc, 0 0 40px #00ffcc; }
    100% { color: #ff6ec7; text-shadow: 0 0 20px #ff6ec7, 0 0 40px #ff6ec7; }
  }

  .stars {
    position: fixed;
    inset: 0;
    pointer-events: none;
  }

  .star {
    position: absolute;
    background: white;
    border-radius: 50%;
    animation: twinkle var(--dur) ease-in-out infinite alternate;
  }

  @keyframes twinkle {
    0%   { opacity: 0.1; }
    100% { opacity: 0.9; }
  }
</style>
</head>
<body>

<div class="stars" id="stars"></div>

<span class="text t1">فجر مدلع</span>
<span class="text t2">فجر مدلع</span>
<span class="text t3">فجر مدلع</span>
<span class="text t4">فجر مدلع</span>
<span class="text t5">فجر مدلع</span>
<span class="text t6">فجر مدلع</span>
<span class="text t7">فجر مدلع</span>

<script>
  const starsContainer = document.getElementById('stars');
  for (let i = 0; i < 150; i++) {
    const star = document.createElement('div');
    star.className = 'star';
    const size = (1 + Math.random() * 2.5) + 'px';
    star.style.cssText = `
      left: ${Math.random() * 100}%;
      top: ${Math.random() * 100}%;
      width: ${size};
      height: ${size};
      --dur: ${(1 + Math.random() * 4)}s;
      animation-delay: ${(Math.random() * 4)}s;
    `;
    starsContainer.appendChild(star);
  }

  document.querySelectorAll('.text').forEach(el => {
    el.addEventListener('click', () => {
      el.style.transition = 'transform 0.6s cubic-bezier(0.36,0.07,0.19,0.97)';
      el.style.transform = 'scale(3) rotate(360deg)';
      setTimeout(() => {
        el.style.transform = '';
        setTimeout(() => { el.style.transition = ''; }, 600);
      }, 600);
    });
  });
</script>
</body>
</html>
